% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ajustar_significancia.R
\name{ajustar_significancia}
\alias{ajustar_significancia}
\title{Ajustar la significancia de un efecto en un diseño experimental}
\usage{
ajustar_significancia(
  data,
  formula,
  term = NULL,
  sig,
  alpha = 0.05,
  magnitud = 2
)
}
\arguments{
\item{data}{Dataframe con los datos del experimento.}

\item{formula}{Fórmula del diseño (ej. \code{y ~ A * B}).}

\item{term}{El término a manipular (ej. "A", "A:B"). Si es NULL, se intenta detectar automáticamente
(priorizando interacciones sobre efectos principales).}

\item{sig}{Lógico. TRUE para buscar diferencias significativas, FALSE para eliminarlas.}

\item{alpha}{Nivel de significancia nominal (solo referecial por ahora, controla la magnitud del cambio).}

\item{magnitud}{Multiplicador del desvío estándar para generar las diferencias. Valor más alto = p-valor más bajo.}
}
\value{
El dataframe modificado.
}
\description{
Modifica los datos de un diseño experimental para garantizar que un término específico
(tratamiento o interacción) resulte significativo (p < alpha) o no significativo,
preservando la estructura de bloques y otros factores.

Utiliza \code{ajustar_variables} internamente para realizar las modificaciones.
}
\examples{
\donttest{
library(stats)

# 1. Caso con energia_abejas: Forzar NO Significativo en Interacción
data(energia_abejas)
mod0 <- aov(Energia ~ Temp_ord * Visc_ord, data = energia_abejas)
summary(mod0)

energia_ns <- ajustar_significancia(energia_abejas, Energia ~ Temp_ord * Visc_ord,
    sig = FALSE
)
summary(aov(Energia ~ Temp_ord * Visc_ord, data = energia_ns))

# 2. Caso con sal_suelo: Forzar NO significativo en interacción específica
data(sal_suelo)
mod_sal <- aov(EC ~ Salinidad_ord * Agua_ord + Textura, data = sal_suelo)
summary(mod_sal)

sal_ns <- ajustar_significancia(sal_suelo, EC ~ Salinidad_ord * Agua_ord + Textura,
    term = "Salinidad_ord:Agua_ord",
    sig = FALSE
)
summary(aov(EC ~ Salinidad_ord * Agua_ord + Textura, data = sal_ns))
}
}
